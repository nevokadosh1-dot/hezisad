# GUI + Windows build dependencies (install IN ADDITION to requirements.txt)
PySide6>=6.6
pyinstaller>=6.3

# Optional: only needed to embed an icon in the .exe (build_windows.py
# converts assets/icon.svg -> icon.ico when Pillow is available).
# pillow>=10.0
